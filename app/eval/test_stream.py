from app.services.llm_service import stream_generate_text


for token in stream_generate_text("What is Python?"):
    print(token, end="", flush=True)


























































































































